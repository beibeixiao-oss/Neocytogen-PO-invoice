# 更新说明 · 2026-09-11.3

请把整个目录（全部 .py 文件）覆盖，版本号必须一致，否则界面会报「文件版本不一致」。

## 2026-09-11.3 本轮修复

用户反馈一个真实案例：同一 PO（PONCG202601009，Lip Laundry 洗衣发票）下两张发票都没能识别出发票号（这两张是洗衣店的扫描小票，本身就没有印发票号字段），台账里这个 PO 恰好也有两行未开票记录。两张都被判成「金额不符」，Note 里写着「台账 146.39 vs 发票 77.5」——用户核对后发现台账里其实就是那两行、金额也完全对得上其中一张，看不懂 146.39 是哪来的。

### 同一 PO 下多张「发票号未识别」的发票，被拿去跟台账整组的合计比，而不是各自比对应的那一行

根因：发票号没抽到时，程序退回按 PO 号匹配；如果同一 PO 下有不止一张这样的发票（这次是 2 张），原有逻辑会先试「分批开票」——把这几张发票的金额加总，跟台账该 PO 下所有未认领行的合计比，加总对得上就整组算 matched。这次加总对不上（77.5 + 68.5 = 146.0，台账合计 146.39，差 0.39——其中一张发票本身就有点小出入，见下条），于是退回到「逐张比」，但逐张比时仍然是拿每一张单独的发票去跟台账里**没认领的全部行的合计**比，而不是去比对它真正对应的那一行。结果两张发票（其中一张其实和台账某一行金额完全一致）都被报成「金额不符：台账 146.39 vs 发票 XX」——这个 146.39 是两行台账的合计，不是任何一张发票该对的数，看起来莫名其妙还互相连累。

现在改成：当同一 PO 下「发票号未识别」的发票张数，正好等于台账里该 PO 未认领的行数时，按金额从小到大依次挑「最接近的一行」做一对一配对（贪心就近匹配），配对后再各自判断是否在容差内。这样报出来的差额就是这一张发票真正对应哪一行、差多少，不会因为同组里另一张对不上就被一起连累成 mismatch。张数和台账行数对不上时（更常见、更该交人工判断的情况），退回原来的整组比较，不做这个配对，避免瞎猜配错。

用重建的台账 + 用户提供的两张真实 PDF 端到端验证：9 件那张（发票印的合计 77.50）现在正确配到台账里 77.50 那一行，判定 matched；8 件那张才会被单独报出「金额不符：台账 68.89 vs 发票 68.5（差 -0.39）」。

### 顺带核实：8 件那张的 0.39 差额是真实业务问题，不是抽取错误

用 OCR 读了这张洗衣发票的原始图像文字：发票上印的确实是 `Subtotal 63.20` + `GST +9% $5.69` + `Rounding - $0.0)` + `Total $68.50`——供应商自己在小票上做了取整/折让，印出来的合计本来就是 68.50，不是 68.89。台账记录的 68.89 大概率是按「税前 63.20 + 税额 5.69」直接相加录入的，没算上洗衣店自己那笔取整折让。这不是程序读错数字，是发票印刷金额与台账录入金额本身有 0.39 的出入，值得让人核实一下要不要按发票上的实收金额（68.50）改台账，还是这笔折让本来就该由供应商另开credit note。

版本号统一改为 `2026-09-11.3`。

## 2026-09-11.2 本轮修复

用户又上传了 16 张此前识别有问题的发票（Atlantis Bioscience、Vazyme、Lonza、Newtonoptic、UPS 进口关税单、BioBasic、Afirmus、Axil、Genomax、Acoerela、BioLabs、ThermoFisher、DMEM Atlantis 等），逐张核对后修了 2 类新问题，另外按用户要求调整了 Proforma 的处理方式：

### 1. 正文抓到的 PO 号不是 PONCG 格式时，没有优先信文件名（影响 UPS 进口关税发票）

UPS 的 Import Tax Invoice 里没有真正的「PO」字段，货运明细表里的 "Reference No."（货运公司自己的运单参考号 `5282293306`）被 `po_no` 的通用兜底正则当成了 PO，覆盖掉了本该来自文件名的正确 PO `PONCG202601011`。PONCG 格式本来就是代码里公认「最可靠」的一条规则，现在改成：正文抽到的 PO 不是 PONCG 格式、而文件名给的是合法 PONCG 格式时，优先信文件名，并在 Note 里注明「很可能抓错了字段」。

（顺带确认了同一张发票「税率异常：89.0%」不是 bug——它是进口关税发票，这次账单本来就只收「代缴关税 GST + 一笔小额垫付手续费」，税额占比天然畸高，程序把它标进待核查是对的，不用处理。）

### 2. 原生 PDF 词间没有空格，"TOTAL SGD" 被读成粘连的 "TOTALSGD"（影响 Acoerela）

Acoerela 这张不是扫描件，是 PDF 本身字符间距太紧，pdfplumber 判断不出词间空格，"TOTAL SGD 400.00" 被抽成一整块 "TOTALSGD 400.00"。所有 `\bTOTAL\b` 开头的规则都要求 TOTAL 后面是词边界，"S" 紧跟着没有边界，全部规则都失效，这张之前完全没抽到发票合计。加了一条专门认「TOTAL 紧贴着币种代码」这种粘连写法的规则，不改全局分词阈值（那样风险面太大）。

### 3. 按用户要求：Proforma 不再单独进「待核查」

之前只要 `is_proforma` 为真，`flag_suspicious` 就会加一条「该 PO 目前只有 Proforma，尚无 Tax Invoice」，把发票推进待核查列表——哪怕金额、格式全部正常。Proforma 本来就正常参与对账（见 README），现在不再仅因为是 Proforma 就额外增加疑点，真正该被挑出来复核的还是金额等式、税率、OCR/AI 来源这些信号。「已忽略的 Proforma」那张 sheet（同一 PO 已有 Tax Invoice 时只算 Tax Invoice）不受影响，仍然保留，那是防止同一笔账被算两次，跟这次的调整是两回事。

### 已知但没有改的问题

至少两张发票（`Magnetic stirrer bars, BioLabs` 和 `AgeI, BioBasic`）的供应商名里混进了品名/货号，因为文件名括号里的内容被上传流程处理掉了逗号，无法确认原始分隔符是逗号还是别的写法，没有把握动这条规则，暂时没改。这不影响对账匹配本身（匹配靠发票号/PO，不靠供应商名字符串），只是显示的供应商名不够干净，之后如果方便的话可以把这两张原始文件名核实一下。


用户上传了 5 张落在「待核查」里的真实发票（Vazyme×2、Sigma、Atlantis、Newtonoptic），逐张排查后修了四类问题：

### 1. OCR 号码纠正只用来配对，没有写回显示值

`INV260123-LBHOO1`（OCR 把 `LBH001` 的两个 0 认成了字母 O）在 `reconcile.py` 里其实已经能靠折叠匹配对上台账的 `INV260123-LBH001`，但只是内部认领时折叠比较，`inv["invoice_no"]` 本身从来没被改过 —— outcome.xlsx 和「待核查」表里显示的仍是那个读错的号码，还会被 `flag_suspicious` 的格式比对误判成「发票号格式与台账不一致」，看起来像是没修好。

现在折叠匹配成功后会把 `inv["invoice_no"]` 换成台账里的正确写法，并在 warnings 里留痕「发票号经 OCR 字符纠正：X → Y」。这样输出表格里看到的就是正确号码，「待核查」里也不会再出现那条格式不一致的假警报。

### 2. OCR 把连字符认成波浪号，发票号被从中间截断（影响 Atlantis 等）

Atlantis 那张扫描件里 `Invoice Number` 被 OCR 读成 `INV-26~03285`，波浪号不在发票号正则的字符集 `[\w\-/]` 里，捕获组匹配到波浪号就直接停了，抽出来的号码变成半截的 `INV-26`——因为形状本身合法（够长、含数字），`valid_invoice_no` 照样放行，问题完全不报错，只在「待核查」里表现成「格式与台账不一致」。

现在 OCR 文本进正则之前会先把「数字/字母之间夹着的波浪号」统一替换回连字符（真实发票文本里波浪号本来就不会出现在这种位置），号码能完整抽出来了：`INV-26-03285`。

### 3. Total 金额和其他文字被 OCR 挤到同一行，正则找不到（影响两张 Vazyme）

两张 Vazyme 扫描件里，"Total 279.04" 和左边不相关的银行信息被 OCR 识别成同一行（`...Singapore Branch Total 279.04`），原来的 TOTAL 正则要么要求行首、要么要求紧跟着币种代码，都对不上这种「total 混在别的文字后面」的情况，导致这两张此前完全没抽到发票合计。

加了一条更宽松的兜底正则：只要求数字紧跟在 `Total` 后面、不在被明确排除的词（EXCL/DISCOUNT 等）后面，不再要求行首或币种。排除词沿用了此前 Lonza/Genomax 两个坑已经验证过的那一套，没有放松这两处。

### 4. 「标签在上、数值在下一行」的版式，Total 一直没有兜底（影响 Sigma）

Sigma 是系统生成的发票，版式是 `... TOTAL CURRENCY` 一行，真正的金额 `601.35 SGD` 在下一行同一列，这种布局所有 TOTAL 正则都覆盖不到（都要求标签和数字在同一行）。

复用了已有的 `_label_below`（原本只给发票号/日期等表头字段用的「同列取值」逻辑）加了一层兜底，专门在其他规则都失败后去找 TOTAL 标签正下方同一列的数字。

调试过程中发现新加的第 3 条兜底正则一开始会被 Sigma 的 `Items Sub-Total 551.70` 误伤——原有的 `Sub`/`Sub ` 两种排除写法没覆盖 `Sub-Total`（连字符分隔）这第三种写法，字符边界正好让 `\bTotal\b` 从连字符缝隙里钻过去，把行项目小计当成了总额。已经把 `Sub-` 也加进排除列表，用真实文件验证过，现在取到的是正确的 601.35，而不是错误的 551.70。

## 之前的修复

以下为 2026-08-21.14 版本的记录：

从 43 张匹配到 51 张，修的不是一个大问题，而是七个互不相关的小 bug。

### 1. 文件名 PO 回退从未生效（影响 15 张）

`\b(PONCG\d{9,})\b` 中的 `\b` 用错。文件名形如 `PONCG202601008_-_...`，
下划线属于单词字符，数字与 `_` 之间不存在词边界，正则一次都没匹配成功过。

改为 `(?<![A-Za-z0-9])(PONCG\d{9,})(?!\d)`。

受影响的是正文没印 PO 的发票：LKC、AsiaMedEnviro、Linde（印的是
`Customer PO No: . EMAIL`），加上全部扫描件。修复后 PO 识别 0/39 → 39/39。

### 2. 扫描件判定用了 `not full_text.strip()`（影响 13 张）

扫描件常被 pdfplumber 读出 1~3 个杂字符（页边框线、噪点被当成字形），
`strip()` 后非空，OCR 分支直接跳过，所有字段静默返回 None 且无任何提示。

改为按每页平均字符数判定（`MIN_CHARS_PER_PAGE = 40`）。

### 3. `SubTotal` 里藏着 `Total`（影响 6 张）

`TOTAL\s*(?:SGD|USD...)` 会匹配 `SubTotal SGD 416.00` 中的
`Total SGD 416.00`，把税前小计当成税后总额。

Linde 453.44 读成 416.00、LKC 190.75 读成 175.00、
Lonza 1281.84 读成 1176.00、Aik moh 147.15 读成 135.00。

加 `(?<!Sub)` 与 `(?<!Sub )`（AsiaMedEnviro 印的是带空格的 `Sub Total`）。
同时排除 `Untaxed Amount`（Roylab）和 `TOTAL EXCL. GST`（Lonza）。

### 4. `Total Discount 0.00` 被当成合计（影响 1 张）

Genomax 的合计读成 0.00，随后等式反推把 `amount_excl` 算成 **-15.84** ——
一个负数金额通过了校验进入对账。

两处修复：标签含 DISCOUNT / PAID / UNITS / QTY 的排除；
合计 ≤ 0 一律判为「未抽到」，宁可空着走兜底也不让假数字进对账。

### 5. `TOTAL (SGD) 305.20` 括号币种不识别（影响 2 张）

Atlantis 两张 Proforma 合计一直是 None。正则只认 `TOTAL SGD`，
补上括号与冒号的写法。

### 6. OCR 字符混淆导致匹配失败（影响 2 张）

`INV260123-LBHOO1` vs 台账 `INV260123-LBH001`。该号格式合法、
`valid_invoice_no` 能过，所以 `fix_ocr_digits` 不触发；精确匹配又对不上，
直接掉进「查无此单」。

修复放在匹配阶段（`reconcile.py`）：对 OCR 来源的发票号做
O/0、I/l/1、S/5、B/8 折叠，**折叠后在台账中唯一命中才认**，
命中多条宁可不认，交人工。Note 会写明纠正前后。

### 7. PO 回退时未扣除已认领的台账行（影响 2 组）

同一 PO 下多张发票时，若其中一张已按发票号认领了对应台账行，
剩下那张仍拿台账整组去比，差额正好是别人那份，报出的
「台账 577.7 vs 发票 957.52」毫无参考价值。

改为只跟尚未被认领的行比。同时把「已认领」的计算移到主循环之前预扫 ——
否则结果取决于遍历顺序，同 PO 两张发票谁先处理结果就不一样。

### 8. 分批开票先加总再比

同一 PO 多张发票时，先把发票合计加起来与台账整组比：
- 加总相符 → 整组算 matched，Note 注明「共 N 张分批开具，合计 X 与台账一致」
- 加总不符 → 自动退回逐张处理，便于定位是哪一张出问题

有任何一张没抽到合计则不加总（加进去只会得出假总额）。

### 9. 等式校验方向按来源区分

原方向固定用总额倒推税额（`gst = incl - excl`），等于默认总额最可信。
对 OCR 恰恰相反：分项数字位数少、字形简单，比总额可靠；
总额通常是版面最大最花的一行，反而最易认错。

Lip Laundry 收据总额 `68.90` 被读成 `68.50`，而 `63.20 + 5.69 = 68.89`，
与台账仅差 1 分 rounding。固定相信总额就会凭空造出 0.39 的差额。

现在：原生 PDF 用总额倒推税额（行为不变）；OCR 用分项加总覆盖总额。

### 10. 其他

- `ocr.py` 补上 `__version__`。此前 `app.py` 拿它做一致性检查却读不到，
  导致「文件版本不一致」告警从第一天起就一直误报。
- 接上了 `ai_extract`（此前从未被调用过）。扫描件或抽不到关键字段时触发，
  需要 `ANTHROPIC_API_KEY`；模型只负责「读」，金额校验与匹配仍由确定性代码完成。
- Lonza 跨行日期：`Our invoice / <号> dated <日期>` 被换行拆开，
  而正文更靠前处有一行 `dated 27-Jan-2026`（那是 Your order 的日期）。
  加跨行锚定规则并排在通用 Date 规则之前。连字符日期格式（`05-Feb-2026`）一并支持。
- 供应商抬头噪音：排除付款条款段落（cheque / payable / remit / banker 等），
  并支持跨行合并（Agilent 抬头被拆成两行，此前只抽到 `Singapore (Sales) Pte Ltd.`）。

## 回归验证

23 张有原生文字的发票，合计金额全部与原件相符，无负值字段。

## 已知的非程序问题

| 类别 | 数量 | 说明 |
|---|---|---|
| 跨年 PO | 8 | PO 号为 PONCG2025xxxxx，台账行在 2025 年度文件里 |
| pending | 22 | 货未到 / 未开票，不算 mismatch |
| 台账录入错误 | 1 | Atlantis PONCG202602028 第二行 Units 录成 10，实际发票开 8 支 |

跨年那 8 张若要对上，需要同时读入 2025 年度台账（目前界面只支持选一份，
需要改成合并读取）。

## 关于「matched」与「待核查」

这是两个独立维度，一张发票可以同时出现在两处：

- **matched** —— 发票号（或 PO）在台账里找到对应行，且金额对得上
- **待核查** —— 抽取过程有疑点，数字本身可能不可信

51 张 matched 中，13 张的数字来自 OCR，建议抽查；3 张发票号格式偏离
该供应商历史规律，需要确认。「该 PO 目前只有 Proforma」是提醒而非排除 ——
按规则这些 Proforma 正常参与了对账。

## 安全提醒

早前上传的 `.env` 中的 `ANTHROPIC_API_KEY` 已暴露，请到 console 撤销并重新签发。
`.env` 不要提交、不要随脚本分发。
